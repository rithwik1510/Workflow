# Implementation Plans

Generated by the improve skill on 2026-06-12 (audit at commit `0cc44a3`,
v0.1.0-beta.5). Execute **one at a time, in the order below** — that ordering
minimizes merge conflicts (001→003 touch overlapping Rust files) and matches
the operator's "improve one thing at a time without breaking anything" intent.
Each executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done.

The operator delegated plan selection ("pick one thing at a time"), so the
top findings by leverage were planned by default per the improve skill's
non-interactive rule.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 006 | Readiness-gated sequential fleet-revive + single-flight git poller | P0 | M | — | DONE — executed 2026-06-12, commit `07c8005` on worktree branch `worktree-agent-a45c4556ce5ef6f49`, reviewed + approved; awaiting operator merge & GUI smoke test |
| 007 | Visibility-driven render governor (bound cost to what's on screen) | P0 | L | 006 (done); 001 before Phase 2 | PHASE 1 IMPLEMENTED 2026-06-20 (TS-only: render sink + WebGL pool + governor; 30 new unit tests; full suite 365 green, typecheck + build clean). Pending GUI smoke test, then commit. Phase 2 (Rust, folds in 002) deferred. |
| 001 | Move blocking Tauri commands off the main thread | P1 | M | — | TODO |
| 002 | Event-driven PTY flush + reader backpressure | P1 | M | 001 | FOLDED INTO 007 — do NOT implement standalone: its uniform reader-backpressure stalls background agents (violates the "agents always run" invariant). 007 Phase 2 implements 002's condvar+backpressure machinery gated on per-pane visibility. |
| 003 | Kill the whole process tree with Job Objects | P1 | M | 002 | TODO |
| 004 | Scope fs commands + localhost-only preview frames | P2 | L | 001 | TODO |
| 005 | Atomic writes for user files and config | P2 | S | — (land after 004 if both in flight) | TODO |
| 008 | Exact agent-state detection via agent hooks (pane-tagged) | P0 (product) | M | — (005's atomic-write pattern reused for settings.json) | APPROVED — operator-directed 2026-07-02 (not from the 2026-06-12 audit); design locked 2026-07-02 (blocked = hollow accent ring, agent glyphs, roll-ups); spike gate PASSED 2026-07-02; IMPLEMENTED 2026-07-02 on worktree branch `worktree-agent-a5b554edff1026bb4` (7 commits incl. review-pass fixes: your-move view-acknowledgment + permission output-exit; 453 vitest + 55 cargo green, clippy/fmt clean); awaiting operator GUI smoke (live Notification kinds, settings toggle against real ~/.claude) & merge |
| 009 | Agent session resume (banner + opt-in auto-resume) | P0 (product) | M | 008 merged (identity, sessionId, spool events); 006's readiness-gated revive for the auto path | IMPLEMENTED 2026-07-02 (Opus subagent, reviewed by Fable — incl. the paneId-remap bridge that keeps persisted records valid across the rehydrate re-key; auto-resume toggle lives in paneResumeStore, not config.toml, per the reopenLastSession precedent). Merged to `feat/agent-resume-and-editor`; awaiting operator GUI smoke (live restart → banner; auto-resume fleet stand-up) |
| 010 | Code editor in split view → git diff tab (Output layer) | P0 (product) | L | Phase A: none (extends mdStore/editor). Phase B: Phase A; reuses 006 git poller; CREATE_NO_WINDOW on all git spawns | PHASES A+B IMPLEMENTED 2026-07-02 (two sequential Opus subagents, each reviewed by Fable; review-pass fix: hard 10 MB refuse ceiling on editor reads). Deviations accepted: HEAD-only base; Ctrl+Shift+D (Ctrl+D = terminal EOF, shortcuts don't skip .xterm); old/new-text pairs → @codemirror/merge computes the diff. NOTE: ptyStore.setCwd is never fed yet, so repo derivation resolves via session.folderPath in practice. Merged to `feat/agent-resume-and-editor`; awaiting operator GUI smoke |
| 011 | Attention escape — OS toasts + taskbar badge/flash (Beta Loop 1) | P0 (Beta) | M | 008 merged (class-A signals are the ONLY escapers); spike gate on Windows toast/badge behavior | IMPLEMENTED 2026-07-03 (Opus subagent, reviewed by Fable; review fix: synthetic-idle baseline so a first-phase permission escapes; click-routing on the approved flash+badge floor — no reliable desktop toast-click callback in plugin 2.3.3). On `feat/beta-trilogy`; awaiting operator GUI smoke (toast minimized dev+packaged, badge legibility) |
| 012 | Terminal credibility floor — Ctrl+F search, web links, paste guard | P0 (Beta) | S–M | 011's prefsStore (paste toggle); registry.ts owns terminal creation + the paste path | IMPLEMENTED 2026-07-03 (Opus subagent, reviewed by Fable; review fix: file-path paste routed through the guarded chokepoint; opener reused — no new plugin). On `feat/beta-trilogy`; awaiting operator GUI smoke (WebGL search decorations, link clicks, paste dialog) |
| 013 | Task loop — sessions born on worktrees, landed from the diff (Beta Loop 2) | P0 (Beta) | L | 010 (diff tab, run_git); 009 (resume follows into worktrees); phased A (attempts) → B (Land) | PHASES A+B IMPLEMENTED 2026-07-03 (two Opus subagents, each reviewed by Fable; integration fix: hint chip yields the top-center overlay slot to the resume banner). run_git generalized to run_tool for gh; TOCTOU-guarded local merge aborts conflicts. On `feat/beta-trilogy`; awaiting operator GUI smoke (full attempt→land arc on a real repo + refusal surfaces) |
| 014 | Workflow coach — rescue tips + a quiet shelf | P1 (product) | M | — (builds only on shipped surfaces: 008 identity, 011 prefsStore, 012 search/paste, 009/013 overlay grammar, beta.8 split pairs) | IMPLEMENTED 2026-07-10 (two-agent build: Steps 1–2 store/gates/seams + Steps 3–5 surfaces/detectors/docs; TS-only, no Rust). One global annoyance ceiling — ≤1 push/day, ≤2/trailing-7, ≤2 showings/tip, no exemptions — enforced in coachStore; four Phase-A tips (selection rescue + session thrash push; precise-signals + scroll-hunt shelf). Deviation: session-pair chip uses the plan's pre-authorized sidebar-region fallback, not two-row tracking. 798 vitest green (+49), typecheck + build clean. On `feat/workflow-coach`; awaiting Fable review + operator GUI smoke |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) |
REJECTED (one-line rationale).

> **2026-06-12 incident note:** Plan 006 was added after a live freeze-loop on
> the maintainer's machine (8-pane simultaneous revive all auto-running
> `claude`, plus unbounded git-poll queue growth on the main thread). 006 and
> 001 together are the incident fix; 006 is pure TS and can land first.

## Dependency notes

- 002 and 003 both restructure `src-tauri/src/pty.rs`; 001 turns its commands
  `async`. Strict 001 → 002 → 003 ordering avoids three-way conflicts.
- 004 reshapes the fs command bodies that 005 also edits; if both are queued,
  land 004 first and let 005's drift check absorb it (the plan anticipates it).
- Plans 002–005 explicitly list Plan-001/002 drift as EXPECTED in their drift
  checks — an `async` keyword or `PaneBuffer` rename is not a STOP.

## Audited but not planned (valid findings, deliberately deferred)

Candidates for the next `/improve` run or quick manual fixes; recorded so they
aren't re-audited from scratch:

- **StatusBar wide store subscription** — `src/components/StatusBar.tsx:34`
  subscribes to the whole `panes` record; re-renders ~5×/sec per streaming
  pane. S effort: narrow selectors (running-count + focused-pane slice).
- **Eager CodeMirror/markdown bundle** — `App.tsx:23` imports `MdEditor`
  statically; `@codemirror/language-data` parses pre-first-paint. S–M effort:
  `React.lazy` the MD surfaces.
- **Dev-dependency CVEs** — `happy-dom` 15.x has a critical VM-escape
  advisory (test-time only, but CI is the release pipeline); vite/esbuild
  moderates. S effort: bump `happy-dom` to ≥20.8.9, `vite`/`vitest` to
  current; verify suite.
- **No lint/format config & no CLAUDE.md/AGENTS.md** — repo has typecheck +
  tests but no eslint/prettier/biome and no agent-onboarding doc, while plans
  here are executed by agents. S effort each.
- **Dead "Filter & options" button** — `SessionsSidebar.tsx:141-143` renders
  a button with no `onClick`. Remove or wire.
- **Stale README** — README.md:36 still says "side-by-side preview pane"
  (replaced by the view/edit toggle per DESIGN.md §3); project-layout section
  drifted from `src/` reality.
- **Updater signing key location** — `src-tauri/lume-updater.key`
  (passwordless, gitignored) lives in the OneDrive-synced tree. OPERATOR
  ACTION, not an executor plan: move it to offline/secure storage (it is
  already mirrored into the GitHub Actions secret), keep a backup, and never
  let an agent handle the file contents.
- **Direction (maintainer strategy, from the audit):** the CEO-REVIEW's
  "fleet dashboard" (named the 10x product, v0.1.1 mandatory follow-up) is
  still absent; table-stakes terminal features (Ctrl+F scrollback search via
  `@xterm/addon-search`, `addon-web-links`, multiline-paste warning, OS
  notifications when minimized) remain open from the 2026-06-09 review; OSC
  133 integration covers PowerShell-family only (cmd/WSL fall back to cadence
  guessing — WSL bash could get a PROMPT_COMMAND-based injection).

## Findings considered and rejected

- **"Autorun listener dropped after timeout" (orchestrator.ts:267-281)** —
  by design: the 20s reap is documented ("give up rather than type blind");
  not a bug.
- **"Final bytes lost when Channel send fails" (pty.rs flusher)** — when
  `send` errors the channel is gone; nothing could be delivered. The real,
  smaller issue (reader not told to stop) is folded into Plan 002.
- **"attentionTracker re-entrancy / `!` assertions"** — bounded Zustand
  nested-set behavior and a safe has()/get() idiom; the tracker was already
  hardened post-2026-06-09 (throttle, session cache, sustain gate).
- **Modal micro-store factory (settingsModal/shortcutsModal/splitMenu/
  contextMenu)** — stores are semantically different (anchors, toggle,
  payloads); a factory would save ~40 LOC and add indirection. Not worth doing.
- **Pruning @fontsource packages** — all 6 are live via `fontPairs.ts`
  font-pair presets; not dead weight.
- **xterm registry "races"** — JS is single-threaded and the registry guards
  existence on each access; no reachable corruption found.
